version: '3.8'

services:
  fund-advisor:
    build: .
    container_name: fund-advisor
    ports:
      - "8888:8000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
